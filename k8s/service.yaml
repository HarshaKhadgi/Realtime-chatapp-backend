kind: Service
apiVersion: v1
metadata:
  name: realtime-chatapp-backend-service
  namespace: realtime-chatapp
spec:
  selector:
    app: realtime-chatapp-backend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000